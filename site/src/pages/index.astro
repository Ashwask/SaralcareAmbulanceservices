---
import Base from "@/layouts/Base.astro";
import FirstVisitModal from "@/components/FirstVisitModal.astro";
import { getProviders, getHospitals, getReports, badge } from "@/lib/data";

const providers = getProviders();
const hospitals = getHospitals();
const reports = getReports();
const verifiedCount = providers.filter((p) => badge(p) === "verified").length;
const repo = "https://github.com/Ashwask/SaralcareAmbulanceservices";
---

<Base
  title="Saralcare Ambulances — open ambulance directory for India"
  description="An open, verified, phone-by-phone directory of every ambulance provider in India — government 108, private, hospital-owned, charitable. Free under CC BY-NC-SA. Fork-friendly. No ads, no commission."
>
  <FirstVisitModal />

  <!-- Emergency-fast-path bar: anyone in distress can act in 2 taps even before reading anything -->
  <div class="emergency-bar" role="region" aria-label="Emergency shortcuts">
    <span class="emergency-text"><strong>In an emergency:</strong></span>
    <a class="emergency-btn" href="tel:108">📞 Call 108</a>
    <a class="emergency-btn alt" href="/find">→ Find an ambulance near me</a>
  </div>

  <section class="hero">
    <p class="kicker">Open public-good infrastructure for India</p>
    <h1 class="mission-title">
      Every ambulance in India.
      <span class="mission-title-2">One open directory.</span>
    </h1>
    <p class="lede">
      Government 108, private operators, hospital-owned fleets, charitable services — published openly, verified by phone, free for any non-commercial use. No ads. No commission. No paid placement.
    </p>

    <div class="hero-ctas">
      <a class="cta-primary" href="/find">
        <span>Find an ambulance</span>
        <span class="cta-sub">Search by location or pincode</span>
      </a>
      <a class="cta-secondary" href="/open-source">
        <span>Contribute</span>
        <span class="cta-sub">Add a provider, verify, fork</span>
      </a>
    </div>

    <p class="micro">Or jump in: <a href="/find">find a provider</a> · <a href="/explore">explore data</a> · <a href="/for-providers">register your ambulance</a> · <a href="/notice">how this works</a>.</p>
  </section>

  <section class="stats" aria-label="Project statistics">
    <div><div class="stat-num">{providers.length}</div><div class="stat-label">providers</div></div>
    <div><div class="stat-num">{hospitals.length}</div><div class="stat-label">hospitals</div></div>
    <div><div class="stat-num">7</div><div class="stat-label">metros + 12 states</div></div>
    <div><div class="stat-num">CC&nbsp;BY-NC-SA</div><div class="stat-label">data licence</div></div>
  </section>

  <section class="why">
    <h2>Why this exists</h2>
    <p>
      In an Indian emergency you'll usually hear about <strong>one</strong> ambulance — the one your hospital ER routes to, the one Google ads at the top, the one a relative happens to know. There are typically <em>fifty</em> alternatives nearby. They go unheard because no neutral party publishes the directory.
    </p>
    <p>
      <strong>The market can't build it.</strong> Aggregators (RED.Health, Medulance, Dial4242) won't list competitors. 108 won't surface private alternatives. JustDial monetises ads, not accuracy. Hospitals route to who pays them.
    </p>
    <p>
      <strong>The state can't build it alone.</strong> NHM Karnataka publishes nothing about station locations, fleet capacity, or response times. ABDM has no EMS layer on the roadmap until at least 2027.
    </p>
    <p>
      <strong>So we're building it as open civic infrastructure.</strong> The data is open. The methodology is published. The decisions are in <a href={`${repo}/tree/main/decisions`}>ADRs</a>. The moderation queue is <a href="/moderation">public</a>. Anyone can fork, mirror, audit, or extend.
    </p>
  </section>

  <section class="three-cards">
    <a class="role-card" href="/find">
      <div class="role-emoji">🚑</div>
      <h3>For people in distress</h3>
      <p>Open the directory, geolocate, see the five nearest providers, tap to call. Works offline once visited. Save numbers for next time. No login, no PII collected.</p>
      <span class="role-cta">Find an ambulance →</span>
    </a>
    <a class="role-card" href="/for-providers">
      <div class="role-emoji">🩺</div>
      <h3>For ambulance providers</h3>
      <p>Free listing under the public-good licence. Earn a verified badge. Claim your record, submit corrections. No payments, no preferential placement — just neutral visibility.</p>
      <span class="role-cta">List your service →</span>
    </a>
    <a class="role-card" href="/open-source">
      <div class="role-emoji">⌬</div>
      <h3>For contributors</h3>
      <p>The whole project lives on GitHub — code, data, ADRs, moderation queue, governance, grants tracker. Pick a path: verify a provider, fix a record, translate, fork for your city.</p>
      <span class="role-cta">Open source hub →</span>
    </a>
  </section>

  <section class="what-makes">
    <h2>What makes this open</h2>
    <p class="small">License is the floor, not the ceiling. The engine of open source is process. Here's what's in the open:</p>

    <div class="om-grid">
      <a class="om-tile" href="/v1/providers.json">
        <h4>The data</h4>
        <p>Every record as YAML in Git + JSON via API. Source-cited per field. CC BY-NC-SA 4.0.</p>
      </a>
      <a class="om-tile" href={`${repo}/tree/main/schema`}>
        <h4>The schemas</h4>
        <p>JSON Schema 2020-12. Validated in CI on every PR. Machine-readable.</p>
      </a>
      <a class="om-tile" href={`${repo}/blob/main/CONTRIBUTING.md#the-verification-phone-call-script`}>
        <h4>The methodology</h4>
        <p>Verification phone-call script. 30-day cycle. Every contributor follows the same protocol.</p>
      </a>
      <a class="om-tile" href={`${repo}/tree/main/decisions`}>
        <h4>The decisions</h4>
        <p>4 ADRs document why CC BY-NC-SA, why pseudonymous, why no GPS, why pan-India.</p>
      </a>
      <a class="om-tile" href="/moderation">
        <h4>The moderation queue</h4>
        <p>Live from GitHub Issues API. See exactly what's pending, oldest first, with SLA flags.</p>
      </a>
      <a class="om-tile" href={`${repo}/blob/main/GRANTS.md`}>
        <h4>The funding</h4>
        <p>Public grants tracker. Section 8 entity registration tracked. Donations not yet accepted — and we say why.</p>
      </a>
      <a class="om-tile" href="/federation">
        <h4>The federation</h4>
        <p>Mirror manifest + fork-your-city template. The project shouldn't depend on one domain.</p>
      </a>
      <a class="om-tile" href={`${repo}/blob/main/CODE_OF_CONDUCT.md`}>
        <h4>The conduct</h4>
        <p>Contributor Covenant adapted for civic-data work. Reporters protected. Decisions logged.</p>
      </a>
    </div>
  </section>

  <section class="not-doing">
    <h2>What we are <em>not</em></h2>
    <ul>
      <li>Not an ambulance service. We own no vehicles, dispatch no calls.</li>
      <li>Not a real-time tracker. We don't claim to know which vehicle is closest <em>right now</em>.</li>
      <li>Not an ads or lead-gen business. No provider pays us, ever.</li>
      <li>Not a substitute for 108. In any medical emergency, dial <a href="tel:108"><strong>108</strong></a> first.</li>
    </ul>
    <p class="small">Read the full <a href="/disclaimer">disclaimer</a> for the legal framing. Read <a href="/notice">how this site works</a> for the user-friendly version.</p>
  </section>

  <section class="contribute-cta">
    <h2>Help build it</h2>
    <p>The directory is built one phone call at a time. The fastest way to help is to pick a provider you've actually called, and tell us what happened.</p>
    <div class="contribute-row">
      <a class="cta-primary" href={`${repo}`}>⌬ Open the repo</a>
      <a class="cta-secondary" href={`${repo}/issues/new/choose`}>+ File an issue</a>
      <a class="cta-secondary" href={`${repo}/blob/main/CONTRIBUTING.md`}>📖 Verification protocol</a>
      <a class="cta-secondary" href={`${repo}/blob/main/CITY-TEMPLATE.md`}>🌍 Fork for your city</a>
    </div>
  </section>

  <style>
    .emergency-bar {
      display: flex; align-items: center; gap: 0.55rem;
      padding: 0.65rem 0.9rem; margin: 0.4rem 0 1.2rem;
      background: var(--accent-bg); border: 1px solid #fecaca;
      border-radius: 12px; flex-wrap: wrap;
      color: #7f1d1d; font-size: 0.92rem;
    }
    .emergency-text { font-weight: 500; }
    .emergency-btn {
      display: inline-flex; align-items: center; gap: 0.4rem;
      padding: 0.45rem 0.85rem; border-radius: 8px;
      background: var(--accent); color: white; font-weight: 600;
      text-decoration: none; font-size: 0.92rem;
    }
    .emergency-btn:hover { background: var(--accent-hover); }
    .emergency-btn.alt { background: transparent; color: var(--accent); border: 1px solid var(--accent); }
    .emergency-btn.alt:hover { background: var(--accent); color: white; }

    .hero { padding: 1rem 0; max-width: 64ch; }
    .kicker {
      font-size: 0.78rem; font-weight: 600; letter-spacing: 0.08em;
      text-transform: uppercase; color: var(--accent);
      margin: 0 0 0.6rem;
    }
    .mission-title {
      font-size: clamp(1.9rem, 5vw, 2.7rem);
      line-height: 1.1; letter-spacing: -0.025em;
      margin: 0 0 0.8rem; font-weight: 800;
    }
    .mission-title-2 { display: block; color: var(--muted); font-weight: 600; }
    .lede { font-size: 1.05rem; line-height: 1.55; color: var(--fg); max-width: 56ch; }

    .hero-ctas { display: flex; gap: 0.6rem; margin-top: 1.2rem; flex-wrap: wrap; }
    .cta-primary, .cta-secondary {
      display: inline-flex; flex-direction: column; align-items: flex-start; gap: 0.1rem;
      padding: 0.85rem 1.2rem; border-radius: 12px;
      text-decoration: none; font-weight: 700; font-size: 1.02rem;
      min-width: 200px;
    }
    .cta-primary { background: var(--accent); color: white; }
    .cta-primary:hover { background: var(--accent-hover); }
    .cta-secondary { background: var(--bg-elev); color: var(--fg); border: 1px solid var(--border); }
    .cta-secondary:hover { border-color: var(--accent); }
    .cta-sub { font-weight: 500; font-size: 0.82rem; opacity: 0.85; }

    .micro { margin-top: 1.1rem; font-size: 0.88rem; color: var(--muted); }

    .stats {
      display: grid; grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
      gap: 0.6rem; margin: 2rem 0 0;
    }
    .stats > div {
      background: var(--bg-elev); border: 1px solid var(--border);
      border-radius: 12px; padding: 0.85rem 1rem;
    }
    .stat-num { font-size: 1.5rem; font-weight: 700; color: var(--fg); }
    .stat-label { font-size: 0.78rem; color: var(--muted); margin-top: 0.15rem; }

    .why { margin-top: 2.5rem; max-width: 64ch; }
    .why h2 { font-size: 1.6rem; letter-spacing: -0.015em; }
    .why p { font-size: 1rem; line-height: 1.6; }

    .three-cards {
      display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 0.7rem; margin-top: 2rem;
    }
    .role-card {
      display: flex; flex-direction: column;
      padding: 1.1rem 1.15rem 1rem;
      background: var(--bg-elev); border: 1px solid var(--border);
      border-radius: 14px;
      text-decoration: none; color: var(--fg);
    }
    .role-card:hover { border-color: var(--accent); }
    .role-emoji { font-size: 1.8rem; margin-bottom: 0.4rem; }
    .role-card h3 { font-size: 1.08rem; margin: 0 0 0.5rem; }
    .role-card p { font-size: 0.92rem; color: var(--muted); margin: 0 0 0.75rem; flex: 1; line-height: 1.5; }
    .role-cta { color: var(--accent); font-weight: 600; font-size: 0.95rem; }

    .what-makes { margin-top: 2.5rem; }
    .what-makes h2 { font-size: 1.6rem; }
    .om-grid {
      display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
      gap: 0.55rem; margin-top: 1rem;
    }
    .om-tile {
      display: block;
      padding: 0.85rem 0.95rem; background: var(--bg-elev);
      border: 1px solid var(--border); border-radius: 10px;
      text-decoration: none; color: var(--fg);
    }
    .om-tile:hover { border-color: var(--accent); }
    .om-tile h4 { margin: 0 0 0.3rem; font-size: 0.94rem; color: var(--accent); }
    .om-tile p { margin: 0; font-size: 0.84rem; color: var(--muted); line-height: 1.45; }

    .not-doing { margin-top: 2.5rem; max-width: 64ch; }
    .not-doing ul { padding-left: 1.2rem; }
    .not-doing li { margin: 0.4rem 0; }

    .contribute-cta { margin-top: 2.5rem; max-width: 64ch; }
    .contribute-row { display: flex; gap: 0.5rem; flex-wrap: wrap; margin-top: 0.8rem; }
    .contribute-row .cta-primary, .contribute-row .cta-secondary {
      flex-direction: row; align-items: center; min-width: 0; font-size: 0.94rem;
      padding: 0.6rem 1rem;
    }
  </style>
</Base>
